name: Publish

on:
  push:
    tags:
      - 'v*'
  release:
    types: [published]

concurrency:
  group: publish-${{ github.event_name == 'push' && github.ref_name || github.event.release.tag_name }}
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.1
        with:
          ref: ${{ github.event_name == 'push' && github.ref || github.event.release.tag_name }}

      - name: Setup project
        uses: ./.github/actions/setup-project

      - name: Verify release version
        env:
          RELEASE_TAG: ${{ github.event_name == 'push' && github.ref_name || github.event.release.tag_name }}
        run: |
          package_version=$(node -p "require('./package.json').version")
          if [ "$RELEASE_TAG" != "v$package_version" ]; then
            echo "Release tag $RELEASE_TAG does not match package version v$package_version." >&2
            exit 1
          fi

      - name: Check package
        run: |
          vp check
          vp test
          vp pack

      - name: Publish with provenance
        working-directory: ${{ runner.temp }}
        run: npm publish "$GITHUB_WORKSPACE" --access public --provenance
